name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - name: Build wasm package
        run: sh scripts/build-wasm.sh

      - name: Prepare site
        run: |
          rm -rf dist
          mkdir -p dist/pkg/compiler
          cp index.html dist/
          cp pkg/compiler/js_token_bin.js dist/pkg/compiler/
          cp pkg/compiler/js_token_bin_bg.wasm dist/pkg/compiler/
          cp pkg/compiler/js_token_bin.d.ts dist/pkg/compiler/
          cp pkg/compiler/js_token_bin_bg.wasm.d.ts dist/pkg/compiler/
          cp pkg/compiler/package.json dist/pkg/compiler/
          touch dist/.nojekyll

      - name: Verify site payload
        run: |
          test -f dist/index.html
          test -f dist/pkg/compiler/js_token_bin.js
          test -f dist/pkg/compiler/js_token_bin_bg.wasm
          find dist -maxdepth 3 -type f | sort

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          force_orphan: true
